---
phase: 05-controlled-markdown-reading-baseline
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01-PLAN.md
files_modified:
  - Frontend/src/modules/pages/reading-material-setup/index.tsx
  - Frontend/src/modules/pages/experiment/components/experiment-stepper.tsx
  - Frontend/src/modules/pages/reading/lib/useReadingSettings.ts
  - Frontend/src/redux/api/reading-material-api.ts
autonomous: true
requirements:
  - READ-01
  - READ-02
  - READ-04
must_haves:
  truths:
    - "The researcher can distinguish local reading edits, reusable saved setups, and the authoritative session baseline."
    - "Applying a reading setup preserves the controlled typography/spacing condition instead of relying on hidden browser residue."
    - "The saved/setup workflow makes lock semantics visible before session start."
  artifacts:
    - path: "Frontend/src/modules/pages/reading-material-setup/index.tsx"
      provides: "Researcher-facing baseline authoring and preview workflow"
    - path: "Frontend/src/modules/pages/experiment/components/experiment-stepper.tsx"
      provides: "Authoritative baseline save/apply flow inside experiment setup"
    - path: "Frontend/src/modules/pages/reading/lib/useReadingSettings.ts"
      provides: "Reduced drift between local preview helpers and active-session truth"
---

<objective>
Make the researcher setup flow explicitly about defining and saving a controlled reading baseline.

Purpose: strengthen the authoring and apply-to-session workflow so saved setups, local previews, and authoritative session state are clearly separated.
Output: a clearer reading-material setup flow and experiment stepper baseline-save story.
</objective>

<context>
@.planning/phases/05-controlled-markdown-reading-baseline/05-CONTEXT.md
@.planning/phases/05-controlled-markdown-reading-baseline/05-RESEARCH.md
@Frontend/src/modules/pages/reading-material-setup/index.tsx
@Frontend/src/modules/pages/experiment/components/experiment-stepper.tsx
@Frontend/src/modules/pages/reading/lib/useReadingSettings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clarify the reading-material setup workflow</name>
  <files>Frontend/src/modules/pages/reading-material-setup/index.tsx, Frontend/src/modules/pages/reading/lib/useReadingSettings.ts</files>
  <action>Refine the reading-material setup UX so it reads like “define a controlled baseline, preview it, and save it” rather than a loose customizer. Make lock/editability and saved-baseline semantics clearer, and keep local preview helpers from looking like session authority.</action>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>The reading-material setup page communicates controlled-condition authoring clearly.</done>
</task>

<task type="auto">
  <name>Task 2: Align experiment setup with the saved baseline story</name>
  <files>Frontend/src/modules/pages/experiment/components/experiment-stepper.tsx, Frontend/src/redux/api/reading-material-api.ts</files>
  <action>Update the experiment stepper so choosing a reading setup, understanding whether it is locked, and saving it into the authoritative session baseline are one coherent story. Keep the Phase 4 draft-versus-authority distinction, but make the controlled-reading terminology clearer.</action>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>The experiment setup flow makes it obvious what baseline will govern the participant view.</done>
</task>

</tasks>

<verification>
Run the frontend production build after each task. This plan is complete only when the researcher can define and apply a controlled baseline without ambiguous ownership.
</verification>

<success_criteria>
The researcher setup flow now clearly expresses what is saved, what is merely local, and whether the participant view is locked or adjustable.
</success_criteria>

<output>
After completion, create `.planning/phases/05-controlled-markdown-reading-baseline/05-02-SUMMARY.md`
</output>
