---
phase: 05-controlled-markdown-reading-baseline
plan: "03"
type: execute
wave: 3
depends_on:
  - 05-01-PLAN.md
  - 05-02-PLAN.md
files_modified:
  - Frontend/src/modules/pages/reading/pages/ReadingPage.tsx
  - Frontend/src/modules/pages/reading/components/ReaderShell.tsx
  - Frontend/src/modules/pages/reading/components/ReadingToolbar.tsx
  - Frontend/src/modules/pages/reading/components/MarkdownReader.tsx
autonomous: true
requirements:
  - READ-01
  - READ-03
  - READ-04
must_haves:
  truths:
    - "The participant route renders backend-owned active session content and presentation during live sessions."
    - "Locked session conditions are enforced by the participant UI rather than merely described elsewhere."
    - "The reading route has first-class missing/loading/degraded states instead of silently substituting mock content."
  artifacts:
    - path: "Frontend/src/modules/pages/reading/pages/ReadingPage.tsx"
      provides: "Stable active-session reader entrypoint"
    - path: "Frontend/src/modules/pages/reading/components/ReaderShell.tsx"
      provides: "Controlled reader surface behavior and lock-aware controls"
    - path: "Frontend/src/modules/pages/reading/components/ReadingToolbar.tsx"
      provides: "Toolbar behavior aligned with controlled session editability"
---

<objective>
Turn the participant route into a stable experiment reading surface.

Purpose: remove ambiguous active-session fallbacks, respect session lock semantics, and make degraded states explicit.
Output: a calmer, more authoritative participant reader.
</objective>

<context>
@.planning/phases/05-controlled-markdown-reading-baseline/05-CONTEXT.md
@Frontend/src/modules/pages/reading/pages/ReadingPage.tsx
@Frontend/src/modules/pages/reading/components/ReaderShell.tsx
@Frontend/src/modules/pages/reading/components/ReadingToolbar.tsx
@Frontend/src/modules/pages/reading/components/MarkdownReader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove ambiguous active-session fallbacks from the participant route</name>
  <files>Frontend/src/modules/pages/reading/pages/ReadingPage.tsx</files>
  <action>When a live session is active, render from the authoritative session baseline only. Replace draft/mock substitutions with explicit loading, unavailable, or missing-baseline states that are safe for researchers and participants to understand.</action>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>The participant route behaves like an experiment surface, not a demo fallback surface.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce controlled presentation conditions in the reader shell</name>
  <files>Frontend/src/modules/pages/reading/components/ReaderShell.tsx, Frontend/src/modules/pages/reading/components/ReadingToolbar.tsx, Frontend/src/modules/pages/reading/components/MarkdownReader.tsx</files>
  <action>Align toolbar and keyboard-based adjustments with the active session’s lock/editability rules. Keep the reader legible and stable while reducing the feel of a freely customizable personal reading app during controlled sessions.</action>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>The participant reader preserves the configured experiment condition instead of letting the UI drift.</done>
</task>

</tasks>

<verification>
Run the frontend production build after each task. This plan is complete only when the participant route behaves like a stable, controlled baseline during active sessions.
</verification>

<success_criteria>
The participant reading surface is authoritative, calm, and lock-aware under live session conditions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-controlled-markdown-reading-baseline/05-03-SUMMARY.md`
</output>
