---
phase: 07-context-preserving-adaptive-reading
plan: "02"
type: execute
wave: 2
depends_on:
  - 07-01-PLAN.md
files_modified:
  - Frontend/src/modules/pages/reading/lib/usePreserveReadingContext.ts
  - Frontend/src/modules/pages/reading/components/ReaderShell.tsx
  - Frontend/src/modules/pages/reading/pages/ReadingPage.tsx
  - Frontend/src/lib/gaze-socket.ts
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
must_haves:
  truths:
    - "Layout-changing interventions preserve the currently read text region using measured anchors, not only coarse scroll restoration."
    - "The participant reader can still produce a continuity result when the gaze-active token is missing or stale."
    - "Every layout-changing presentation update produces a continuity outcome that the backend can inspect."
  artifacts:
    - path: "Frontend/src/modules/pages/reading/lib/usePreserveReadingContext.ts"
      provides: "Measured anchor capture, restore, and status classification logic"
    - path: "Frontend/src/modules/pages/reading/components/ReaderShell.tsx"
      provides: "Reader integration for continuity capture and reporting callbacks"
    - path: "Frontend/src/modules/pages/reading/pages/ReadingPage.tsx"
      provides: "Participant-route wiring that sends authoritative continuity outcomes"
  key_links:
    - from: "Frontend/src/modules/pages/reading/components/ReaderShell.tsx"
      to: "Frontend/src/modules/pages/reading/lib/usePreserveReadingContext.ts"
      via: "capture before presentation change and report after intervention key change"
      pattern: "captureContextAnchor|onContextPreservationChange|interventionKey"
---

<objective>
Harden the participant reader so it preserves place using measured continuity anchors and reports the result after each layout-changing intervention.

Purpose: make context preservation reliable enough to defend at the reader level, not only as a backend contract.
Output: stronger anchor capture, explicit result classification, and participant-route reporting.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-context-preserving-adaptive-reading/07-CONTEXT.md
@.planning/phases/07-context-preserving-adaptive-reading/07-RESEARCH.md
@.planning/phases/07-context-preserving-adaptive-reading/07-01-PLAN.md
@Frontend/src/modules/pages/reading/lib/usePreserveReadingContext.ts
@Frontend/src/modules/pages/reading/components/ReaderShell.tsx
@Frontend/src/modules/pages/reading/pages/ReadingPage.tsx
@Frontend/src/lib/gaze-socket.ts
</context>

<tasks>

<task id="07-02-01" type="auto">
  <name>Task 1: Refactor the context-preservation hook to measure outcome quality explicitly</name>
  <read_first>
    <file>Frontend/src/modules/pages/reading/lib/usePreserveReadingContext.ts</file>
    <file>Frontend/src/modules/pages/reading/components/ReaderShell.tsx</file>
    <file>Frontend/src/modules/pages/reading/pages/ReadingPage.tsx</file>
  </read_first>
  <files>Frontend/src/modules/pages/reading/lib/usePreserveReadingContext.ts</files>
  <action>Refactor `usePreserveReadingContext` so it returns a structured result after each `interventionKey` change. Keep the existing active-token and nearby-token strategy, but add fallback capture modes named `block-anchor` and `scroll-only` when no active token is available. Measure `anchorErrorPx` after restoration, compute `viewportDeltaPx` from the pre-change versus post-change scroll position, and classify the result as `preserved` when the final anchor error is small, `degraded` when restoration succeeds with a larger error or weaker fallback source, and `failed` when no stable anchor can be restored.</action>
  <acceptance_criteria>
    <criterion>`Frontend/src/modules/pages/reading/lib/usePreserveReadingContext.ts` contains `status: "preserved" | "degraded" | "failed"` or the equivalent union.</criterion>
    <criterion>`Frontend/src/modules/pages/reading/lib/usePreserveReadingContext.ts` contains the strings `block-anchor` and `scroll-only`.</criterion>
    <criterion>`Frontend/src/modules/pages/reading/lib/usePreserveReadingContext.ts` computes and returns both `anchorErrorPx` and `viewportDeltaPx`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>The continuity hook produces a measurable outcome instead of silent best-effort restoration.</done>
</task>

<task id="07-02-02" type="auto">
  <name>Task 2: Wire the participant route to send continuity outcomes after layout changes</name>
  <read_first>
    <file>Frontend/src/modules/pages/reading/components/ReaderShell.tsx</file>
    <file>Frontend/src/modules/pages/reading/pages/ReadingPage.tsx</file>
    <file>Frontend/src/lib/gaze-socket.ts</file>
    <file>Frontend/src/modules/pages/reading/lib/usePreserveReadingContext.ts</file>
  </read_first>
  <files>Frontend/src/modules/pages/reading/components/ReaderShell.tsx, Frontend/src/modules/pages/reading/pages/ReadingPage.tsx, Frontend/src/lib/gaze-socket.ts</files>
  <action>Add an `onContextPreservationChange` callback to `ReaderShell` and call it after the hook finishes its restore pass. In `ReadingPage.tsx`, forward that payload to `updateReadingContextPreservation(...)` so the participant route reports each measured continuity outcome. Ensure the callback is only fired for layout-changing presentation updates, not for unrelated gaze or viewport refreshes.</action>
  <acceptance_criteria>
    <criterion>`Frontend/src/modules/pages/reading/components/ReaderShell.tsx` contains `onContextPreservationChange`.</criterion>
    <criterion>`Frontend/src/modules/pages/reading/pages/ReadingPage.tsx` calls `updateReadingContextPreservation(`.</criterion>
    <criterion>`Frontend/src/modules/pages/reading/components/ReaderShell.tsx` references `interventionKey` when deciding when to report continuity outcomes.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>The participant route reports one authoritative continuity result per layout-changing intervention.</done>
</task>

</tasks>

<verification>
Run the frontend production build after each task. This plan is complete only when the participant reader both restores place and reports how well it did.
</verification>

<success_criteria>
The participant reader now provides measured, inspectable continuity outcomes for layout-changing interventions.
</success_criteria>

<output>
After completion, create `.planning/phases/07-context-preserving-adaptive-reading/07-02-SUMMARY.md`
</output>
