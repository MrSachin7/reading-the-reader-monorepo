---
phase: 04-device-setup-calibration-workflow
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - Frontend/src/modules/pages/experiment/components/experiment-stepper.tsx
  - Frontend/src/modules/pages/experiment/components/eyetracker-setup.tsx
  - Frontend/src/modules/pages/experiment/components/calibration-step.tsx
  - Frontend/src/modules/pages/experiment/components/utils.ts
  - Frontend/src/redux/slices/experiment-slice.ts
autonomous: true
requirements:
  - SETUP-02
  - SETUP-04
  - SETUP-05
  - SETUP-06
must_haves:
  truths:
    - "The experiment page reads like one guided researcher workflow instead of separate local-state islands."
    - "The workflow surfaces backend-authoritative blockers and calibration quality before the researcher presses Start."
    - "Local Redux draft state supports form UX but does not redefine whether the workflow is actually ready."
  artifacts:
    - path: "Frontend/src/modules/pages/experiment/components/experiment-stepper.tsx"
      provides: "One authoritative guided setup shell with inline blockers and start gating"
    - path: "Frontend/src/modules/pages/experiment/components/eyetracker-setup.tsx"
      provides: "Device/licence step aligned with authoritative selection state"
    - path: "Frontend/src/modules/pages/experiment/components/calibration-step.tsx"
      provides: "Calibration summary step aligned with validation quality and route return state"
  key_links:
    - from: "Frontend/src/modules/pages/experiment/components/experiment-stepper.tsx"
      to: "Frontend/src/lib/experiment-session.ts"
      via: "setup readiness rendering"
      pattern: "setup|block|currentStep"
    - from: "Frontend/src/modules/pages/experiment/components/calibration-step.tsx"
      to: "Frontend/src/lib/calibration.ts"
      via: "validation quality summary"
      pattern: "quality|passed|completed"
---

<objective>
Refactor the experiment page into one coherent guided setup workflow.

Purpose: use the richer authoritative setup/calibration contracts to show step progress, blockers, calibration quality, and start readiness inside one researcher-operated flow.
Output: updated experiment stepper and step components that follow backend truth while preserving the current app’s researcher-facing visual language.
</objective>

<execution_context>
@C:/Users/s243871/reading-the-reader-monorepo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/s243871/reading-the-reader-monorepo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-device-setup-calibration-workflow/04-CONTEXT.md
@.planning/phases/04-device-setup-calibration-workflow/04-RESEARCH.md
@Frontend/src/modules/pages/experiment/components/experiment-stepper.tsx
@Frontend/src/modules/pages/experiment/components/eyetracker-setup.tsx
@Frontend/src/modules/pages/experiment/components/calibration-step.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework the experiment stepper around authoritative setup readiness</name>
  <files>Frontend/src/modules/pages/experiment/components/experiment-stepper.tsx, Frontend/src/modules/pages/experiment/components/utils.ts, Frontend/src/redux/slices/experiment-slice.ts</files>
  <read_first>
    - Frontend/src/modules/pages/experiment/components/experiment-stepper.tsx
    - Frontend/src/lib/experiment-session.ts
    - Backend/src/core/ReadingTheReader.core.Application/ApplicationContracts/Realtime/ExperimentSessionSnapshot.cs
    - .planning/phases/04-device-setup-calibration-workflow/04-CONTEXT.md
  </read_first>
  <action>Refactor the main experiment stepper so visible step completion, current-step focus, disabled-state logic, and start gating are derived primarily from authoritative experiment/setup snapshots. Preserve local form draft behavior where needed, but stop treating local-only flags as the final source of truth for readiness.</action>
  <acceptance_criteria>
    - `experiment-stepper.tsx` uses authoritative setup readiness fields for step completion and start gating.
    - The stepper shows blocking reasons or status summaries before the user presses Start.
    - Frontend build remains green after the orchestration changes.
  </acceptance_criteria>
  <verify>
    <automated>bun run build --cwd Frontend</automated>
  </verify>
  <done>The experiment page is driven by authoritative setup state rather than scattered local readiness heuristics.</done>
</task>

<task type="auto">
  <name>Task 2: Align the eyetracker and calibration steps with the guided workflow</name>
  <files>Frontend/src/modules/pages/experiment/components/eyetracker-setup.tsx, Frontend/src/modules/pages/experiment/components/calibration-step.tsx, Frontend/src/modules/pages/experiment/components/experiment-stepper.tsx</files>
  <read_first>
    - Frontend/src/modules/pages/experiment/components/eyetracker-setup.tsx
    - Frontend/src/modules/pages/experiment/components/calibration-step.tsx
    - Frontend/src/modules/pages/calibration/index.tsx
    - Frontend/src/redux/api/eyetracker-api.ts
  </read_first>
  <action>Update the device and calibration step components so they display authoritative selection state, licence status, calibration quality, validation pass/fail, and explicit next actions inside the stepper. Keep the calibration page launch action, but make the step clearly explain what will happen, what the last result was, and whether the researcher can proceed.</action>
  <acceptance_criteria>
    - `eyetracker-setup.tsx` no longer relies on a local success assumption after selection without reflecting backend state.
    - `calibration-step.tsx` surfaces the latest authoritative calibration and validation outcome, including quality.
    - The experiment flow makes it obvious why Start is still blocked when calibration or licensing is incomplete.
  </acceptance_criteria>
  <verify>
    <automated>bun run build --cwd Frontend</automated>
  </verify>
  <done>The researcher can understand device readiness, calibration outcome, and remaining blockers from one guided setup page.</done>
</task>

</tasks>

<verification>
Run a frontend production build after each task. This plan is complete only when the experiment page reads like one guided setup path backed by authoritative workflow state.
</verification>

<success_criteria>
The experiment page becomes the clear operator workflow for preparing a session, with explicit setup status and backend-aligned start gating.
</success_criteria>

<output>
After completion, create `.planning/phases/04-device-setup-calibration-workflow/04-03-SUMMARY.md`
</output>
