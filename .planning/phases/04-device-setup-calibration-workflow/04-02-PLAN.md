---
phase: 04-device-setup-calibration-workflow
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - Backend/src/ReadingTheReader.WebApi/EyeTrackerEndpoints/GetConnectedEyetrackersEndpoint.cs
  - Backend/src/ReadingTheReader.WebApi/EyeTrackerEndpoints/SelectEyeTrackerEndpoint.cs
  - Backend/src/ReadingTheReader.WebApi/CalibrationEndpoints/GetCalibrationStateEndpoint.cs
  - Frontend/src/redux/api/eyetracker-api.ts
  - Frontend/src/redux/api/experiment-session-api.ts
  - Frontend/src/lib/experiment-session.ts
  - Frontend/src/lib/calibration.ts
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04
must_haves:
  truths:
    - "Device-selection and calibration transport surfaces return or expose enough authoritative data for the guided workflow to reflect backend setup truth."
    - "Frontend mirrors of setup and calibration state stay aligned with the backend contracts."
    - "The guided workflow no longer depends on transport endpoints that only say '200 OK' while hiding readiness state."
  artifacts:
    - path: "Backend/src/ReadingTheReader.WebApi/EyeTrackerEndpoints/SelectEyeTrackerEndpoint.cs"
      provides: "Authoritative device-selection response behavior"
    - path: "Frontend/src/redux/api/eyetracker-api.ts"
      provides: "Frontend device-selection contract aligned to the backend response"
    - path: "Frontend/src/lib/experiment-session.ts"
      provides: "Mirrored setup readiness/checklist shape for the experiment workflow"
  key_links:
    - from: "Backend/src/ReadingTheReader.WebApi/EyeTrackerEndpoints/SelectEyeTrackerEndpoint.cs"
      to: "Frontend/src/redux/api/eyetracker-api.ts"
      via: "selection response contract"
      pattern: "serialNumber|hasSavedLicence|setup"
    - from: "Backend/src/ReadingTheReader.WebApi/CalibrationEndpoints/GetCalibrationStateEndpoint.cs"
      to: "Frontend/src/lib/calibration.ts"
      via: "calibration summary contract"
      pattern: "quality|passed|validation"
---

<objective>
Align transport and frontend contracts around authoritative setup and calibration state.

Purpose: make the experiment flow consume backend truth for device selection, licence readiness, calibration status, and validation summary.
Output: updated Web API responses plus frontend contract mirrors and API adapters that surface setup workflow data explicitly.
</objective>

<execution_context>
@C:/Users/s243871/reading-the-reader-monorepo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/s243871/reading-the-reader-monorepo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-device-setup-calibration-workflow/04-CONTEXT.md
@.planning/phases/04-device-setup-calibration-workflow/04-RESEARCH.md
@Backend/src/ReadingTheReader.WebApi/EyeTrackerEndpoints/SelectEyeTrackerEndpoint.cs
@Backend/src/ReadingTheReader.WebApi/CalibrationEndpoints/GetCalibrationStateEndpoint.cs
@Frontend/src/redux/api/eyetracker-api.ts
@Frontend/src/lib/experiment-session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Return and expose authoritative device-selection state</name>
  <files>Backend/src/ReadingTheReader.WebApi/EyeTrackerEndpoints/SelectEyeTrackerEndpoint.cs, Backend/src/ReadingTheReader.WebApi/EyeTrackerEndpoints/GetConnectedEyetrackersEndpoint.cs, Frontend/src/redux/api/eyetracker-api.ts</files>
  <read_first>
    - Backend/src/core/ReadingTheReader.core.Application/ApplicationContracts/Realtime/SensingOperations.cs
    - Backend/src/ReadingTheReader.WebApi/EyeTrackerEndpoints/SelectEyeTrackerEndpoint.cs
    - Frontend/src/redux/api/eyetracker-api.ts
    - .planning/phases/04-device-setup-calibration-workflow/04-01-PLAN.md
  </read_first>
  <action>Adjust the device-selection transport so the frontend can consume authoritative post-selection state instead of assuming success from a void response. Preserve the existing upload/save-licence semantics while surfacing the selected tracker summary and any setup-ready fields that the experiment flow needs immediately after selection.</action>
  <acceptance_criteria>
    - `SelectEyeTrackerEndpoint.cs` no longer responds with a bare success-only contract for the main happy path.
    - `eyetracker-api.ts` transforms the authoritative response into typed frontend data instead of ignoring the body.
    - `getEyetrackers` and `selectEyetracker` still support saved-licence visibility per device.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build Backend/reading-the-reader-backend.sln -v minimal && bun run build --cwd Frontend</automated>
  </verify>
  <done>The frontend can immediately consume authoritative device-selection state after the researcher chooses a tracker.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror richer setup and calibration summary contracts on the frontend</name>
  <files>Backend/src/ReadingTheReader.WebApi/CalibrationEndpoints/GetCalibrationStateEndpoint.cs, Frontend/src/redux/api/experiment-session-api.ts, Frontend/src/lib/experiment-session.ts, Frontend/src/lib/calibration.ts</files>
  <read_first>
    - Backend/src/core/ReadingTheReader.core.Application/ApplicationContracts/Realtime/ExperimentSessionSnapshot.cs
    - Backend/src/core/ReadingTheReader.core.Application/ApplicationContracts/Realtime/CalibrationContracts.cs
    - Frontend/src/lib/experiment-session.ts
    - Frontend/src/lib/calibration.ts
  </read_first>
  <action>Update frontend contract mirrors and API slices so the experiment workflow can read richer setup readiness, calibration validation pass/fail, quality, and blocking-reason fields directly from backend-owned contracts. Keep transport and mirror types aligned with the authoritative backend snapshot shapes introduced in `04-01`.</action>
  <acceptance_criteria>
    - `rg -n "setup|block|quality|validation" Frontend/src/lib/experiment-session.ts` matches the enriched workflow fields.
    - `rg -n "quality|passed|averageAccuracy|averagePrecision" Frontend/src/lib/calibration.ts` matches the calibration-summary mirror fields used by the UI.
    - `bun run build --cwd Frontend` passes after the contract changes.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build Backend/reading-the-reader-backend.sln -v minimal && bun run build --cwd Frontend</automated>
  </verify>
  <done>The guided workflow contracts are aligned across backend transport and frontend mirrors before the experiment UI is refactored.</done>
</task>

</tasks>

<verification>
Run a backend build plus frontend build after each task. This plan is complete only when transport responses and frontend mirrors agree on setup and calibration state.
</verification>

<success_criteria>
Phase 4 has stable backend-to-frontend contracts for setup readiness, device selection, and calibration summary before UI orchestration work begins.
</success_criteria>

<output>
After completion, create `.planning/phases/04-device-setup-calibration-workflow/04-02-SUMMARY.md`
</output>
