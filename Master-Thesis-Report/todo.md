# Thesis TODOs

- [ ] **Abstract & Conclusion — name the pillars (written LAST, per `CLAUDE.md` §2.1)**: When written, the Abstract must make the contribution unmissable by naming the headline pillars: the **dual-screen, researcher-led** workflow; **advisory/autonomous + pluggable (incl. external/AI) decisioning**; **context-preserving intervention** (controlled-boundary commit + position anchor, with recovery instrumented, e.g. Reading Resume Time); and the **reproducible session record**. Mirror these in the Conclusion. Context preservation in particular was under-claimed and must appear here.

- [x] **NFR2 — Latency metric**: DONE. NFR2 now fixes the gaze-to-intervention budget at ≤ 100 ms (instantaneous-response threshold, `\cite{miller1968response, nielsen1993responsetime}`) and within a single fixation (200–300 ms, `\cite{rayner1998eyemovements}`). The Evaluation chapter must report the measured value against this 100 ms ceiling.

- [x] **Implement regression detection in the app**: DONE — verified 2026-06-18 that regressions are first-class end-to-end. Computed in `BuiltInEyeMovementAnalysisStrategy.cs:101` (`RegressionCount += saccade.IsRegression ? 1 : 0`); carried as `RegressionCount` on `EyeMovementAnalysisRuntimeState` and the public `EyeMovementAnalysisSnapshot`; flows into the decision context for built-in *and* external providers (`DecisionContextSnapshot.cs:21`, `DecisionContextFactory.cs:33`); exported via `ExperimentReplayExport.cs:487` + the replay serializer; displayed on the frontend (`ReadingDynamicsLegend.tsx`, `SaccadePathOverlay.tsx`, replay metadata). Remaining work is report-only: name it as a first-class event in Ch6 (`lst:impl-saccade` prose) and verify it against RQ2/FR18 in Ch7.

- [ ] **Evaluation §Performance — consume the telemetry sidecar**: The app now persists transport telemetry to a sidecar export. A session writes `experiment-telemetry-<id>.json` (schema `rtr.experiment-telemetry` v1), downloadable at `GET /experiment-session/telemetry`; it holds role-tagged (participant/researcher) samples of `rttMs`, `sampleRateHz`, `validityRate`, plus a `summary` with min/median/p95/max and `overBudgetPct` against the 100 ms budget. When writing the Performance Evaluation, report the participant-role `rttMs` distribution against the NFR2 ceiling. **Precision constraint:** `rttMs` is the WebSocket *transport round-trip*, NOT the full gaze-to-intervention path — describe it as transport RTT and do not claim it is the closed-loop latency unless the loop stages are separately instrumented.

- [ ] **NFR measurability (NFR3, NFR4)**: Consider adding acceptance criteria to NFR3 (Usability) and NFR4 (Reliability) following the same principle as NFR2 — qualitative requirements are harder to defend in evaluation.

- [ ] **Declaration of Use of Generative AI — fill specifics**: The forward references are now in place — `Chapters/01_Introduction/03_contributions.tex` (intro note), `Chapters/03_Methodology/05_tools_and_technologies.tex` (method, points to `app:ai-declaration`), and the appendix `Backmatter/Appendix.tex` exists with `tab:gai-tools`. Remaining: populate `tab:gai-tools` and the appendix prose with the actual tools used, the specific tasks/where, and the validation process, per `CLAUDE.md` §1.4.

- [x] **Ch6 must document the concrete stack and supporting libraries**: DONE 2026-06-22. Added §6.1.1 "Technology stack and dependencies" (`01_structure.tex`) with Table 6.1 (`tab:impl-stack`) recording the verified versions (.NET 10, FastEndpoints 8.0.1, CsvHelper 33.1.0, Tobii.Research.x64 1.11.0.1334; Next.js 16.1.6, React 19.2.3, TypeScript 5.x, Redux Toolkit/RTK Query 2.11.x, Tailwind 4.x; Bun left unversioned because CI uses `bun-version: latest`) and roles, cross-referencing the §5.8 rationale; CI gets a one-line pointer to §6.5 (Engineering Practice), where the fuller build/CI treatment belongs. The Tobii SDK integration (discovery/licence/field-mapping/Windows/resolution) is now §6.3.3 (`subsec:impl-tobii-sdk`). Original note retained below.
  - The per-technology version/library detail was removed from the old Methodology §3.5 (it was architectural/implementation, not method). The *selection rationale* now lives in `Chapters/05_SystemDesign/07_decisions.tex` (`subsec:technology-selection`, `tab:technology-selection`). Ensure `Chapters/06_Implementation` records the concrete realisation that was dropped: versions (Next.js 16, React 19, TypeScript 5, .NET 10, Bun 1.3) and supporting libraries (Redux Toolkit + RTK Query, Tailwind v4, FastEndpoints 8, CsvHelper 33, Tobii.Research.x64 1.11) plus the GitHub Actions CI pipelines.

- [ ] **Mermaid figures — visual polish to match TikZ**: The three Mermaid-rendered figures (`Chapters/05_SystemDesign/adaptive-loop.mmd`, `decision-lifecycle.mmd`, and `session-lifecycle.mmd`, each rendered to a sibling `.pdf`) have a lighter font than the crisp TikZ figures. They can now be regenerated with `bunx -y @mermaid-js/mermaid-cli -i NAME.mmd -o NAME.pdf` (mmdc is reachable via `bunx`). DONE in-session: the "Sensing source (Tobii)" lifeline was renamed to the generic "Eye tracker", the reading surface was split into "Participant reading surface" and "Researcher monitoring surface", and the fixation/saccade decision-support was added. REMAINING: consider a Mermaid theme/config so the font weight matches the TikZ figures (e.g. a `mermaid-config.json` with a serif/heavier theme), or accept the current look.

- [x] **Figure hyphenation polish (minor)**: DONE 2026-06-22. Use-case nodes rebroken to "Define\\Experiment\\Template" and "Start\\Experiment\\Session" (no more "Ex-periment"); Fig 5.6 node "Built-in\\implementation\\in-process" (no more "im-plementation"). The "Deci-sion Provider" glitch was already gone because the old TikZ Fig 5.1 was replaced by the Excalidraw SVG master. Both diagrams re-rendered and verified (no overlap).

- [x] **Analysis algorithm — describe I-AOI/dwell extraction**: DONE 2026-06-22. The Implementation chapter (`06_Implementation/03_highlights.tex`, "Temporal analysis as a replaceable strategy") now names the area-of-interest (I-AOI), dwell-based branch of the Salvucci & Goldberg taxonomy, states explicitly that it uses neither I-VT (velocity) nor I-DT (dispersion), justifies AOI for reading, and reports the real constants (confirmation dwell 90/70/135 ms; attention binning skim 45 / fixation 130 ms). Ch2 (`01_key_concepts.tex:270-271`) gained the I-VT/I-DT/I-AOI abbreviations + explicit non-use. Original note retained below for reference.
  - In System Design and/or Implementation, document that the event-extraction layer uses the area-of-interest (I-AOI) branch of the Salvucci & Goldberg taxonomy, not velocity (I-VT) or dispersion (I-DT). Two stages: (1) frontend hit-tests the gaze point against rendered word boxes to resolve a token (`Frontend/src/modules/pages/reading/lib/useGazeTokenHighlight.ts`); (2) backend thresholds dwell time per token into fixations/saccades with direction (forward/backward=regression/line-change) in `Backend/src/core/ReadingTheReader.core.Application/ApplicationContracts/Realtime/Analysis/BuiltInEyeMovementAnalysisStrategy.cs`. Report the actual threshold constants (SkimThresholdMs=45, FixationThresholdMs=130, Initial/SameLine/NewLine=90/70/135 ms) and justify the AOI choice for reading (maps gaze straight to words, which the decision layer needs). Connects to the framing in `Chapters/02_KeyConceptsAndRelatedWork/01_key_concepts.tex` (Sec.~\ref{subsec:oculomotor-events}).

- [ ] **Verify `Expired` decision-proposal trigger (F4)**: The decision-lifecycle figure (`Chapters/05_SystemDesign/decision-lifecycle.mmd`) draws `Pending -> Expired` ("validity window elapsed"). `Expired` is defined in `Backend/src/core/ReadingTheReader.core.Domain/Decisioning/DecisionProposalStatus.cs` and `DecisionProposalLifecycleRules.CanTransition` permits the transition, but no code that *sets* `Expired` (a timeout/validity-window writer) was found in `ExperimentSessionManager.Decisions.cs`. Confirm whether a proposal-expiry mechanism exists elsewhere; if so keep the transition and cite it in the §5.6 prose, otherwise drop `Expired` from the figure or label it a designed-but-unwired state. Until confirmed, the figure caption / prose must carry `\todo{verify Expired trigger}`.
