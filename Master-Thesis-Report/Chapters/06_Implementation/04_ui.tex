\section{User Interface Realization}
\label{sec:impl-ui}
% Authoring brief for this section lives in section.md (§6.4). Proportionate; screenshots from the user.
\todo[inline]{\S6.4 SLIMMED (decided): the end-to-end flow and the in-context screenshots (reader, console, calibration, stepper, replay) now live in \cref{sec:impl-session}. This section no longer re-walks the screens. Keep it to UI \emph{architecture} realized: the single Next.js front-end split into thin routing (\texttt{app/}) vs feature UI (\texttt{modules/pages}); the two route groups (with-/without-sidebar) realizing the researcher console vs participant reader; the control/presentation separation and where shared realtime state lives (\texttt{lib/gaze-socket}, \texttt{use-live-experiment-session}); cref Ch5 sec:design-extensibility rather than re-arguing. Point the fuller screenshot set to a UI gallery appendix. Do not duplicate \cref{sec:impl-session}'s screenshots.}
